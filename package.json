{
  "name": "landonkea-open-navigation-geospatial-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Open Navigation & Geospatial Platform: a free PWA that shows a group's live location on a shared map during an event, built first for a cycling meetup (bikeMesa) but kept generic underneath so it works for any group event, indoor kiosks included.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.2",
    "maplibre-gl": "^4.7.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "@types/qrcode": "^1.5.6",
    "jsdom": "^30.0.1",
    "typescript": "^5.7.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
